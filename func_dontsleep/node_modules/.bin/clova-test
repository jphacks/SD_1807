#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../love-clova/dist/test/clova-test.js" "$@"
  ret=$?
else 
  node  "$basedir/../love-clova/dist/test/clova-test.js" "$@"
  ret=$?
fi
exit $ret
